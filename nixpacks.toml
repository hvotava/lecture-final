[phases.setup]
nixFile = ".nixpacks/nixpacks.nix"

[phases.install]
cmds = ["npm ci --prefer-offline=false --no-audit --cache /tmp/.npm-cache"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production" 