[providers]
node = "18"

[phases.setup]
nixPkgs = ["nodejs", "npm"]
workDir = "react-dashboard/backend"

[phases.install]
cmd = "npm ci --only=production"

[phases.start]
cmd = "npm start"

[variables]
NODE_ENV = "production" 